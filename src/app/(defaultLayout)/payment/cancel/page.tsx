import Cancel from '@/components/PaymentCancel/page';
import React from 'react';

const page = () => {
    return (
        <div>
            <Cancel></Cancel>
        </div>
    );
};

export default page;